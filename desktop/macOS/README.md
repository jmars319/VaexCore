# macOS Desktop

macOS-specific desktop files live here.

- `assets/icon.icns` is the current macOS app icon generated from `desktop/shared/assets/logo.jpg`.
- `scripts/` contains macOS packaging, unsigned tester artifact, native module install, and release smoke scripts.

The current macOS build remains the known-good baseline before Windows and Linux conversion begins.
